# Demoing vagrant and ansible

# Vagrantfile

- Set up a small network of host-only vms.
- Configure vm properties like assigned memory.
- Configure a guest os with things like secure public-key authentication.